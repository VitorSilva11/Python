# Display on screen and read a variable

number = int(input("Enter a number: "))

# Variables to the calculation

double = number * 2
triple = number * 3
root = number ** (1 / 2)

# Display the variables

print('Double the number is {}'.format(double))
print('Triple the number is {}'.format(triple))
print('Root the number is {}'.format(root))

