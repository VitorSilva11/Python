# Display on the screen and read a variable

number = int(input('Enter a value: '))

# Display the successor and predecessor

print('The predecessor {} and successor {}, of the value {}'.format(number - 1, number + 1, number))
